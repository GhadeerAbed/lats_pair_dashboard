import useSWR, { type Fetcher, KeyedMutator } from "swr";
import useSWRMutation, { type MutationFetcher } from "swr/mutation";

export {
  useSWR,
  useSWRMutation,
  type Fetcher,
  type MutationFetcher,
  type KeyedMutator,
};
